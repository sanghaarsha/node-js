// 'npm install' : this command will detect and download missing dependencies
// it willl look in package.json and package-lock.json
// and install packages accordingly

const chalk = require("chalk");
console.log(chalk.green.bold.inverse("Success!"));
